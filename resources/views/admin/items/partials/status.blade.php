<span class="inline-flex items-center px-3 py-1 text-xs font-semibold rounded-full {{ $item->enabled ? 'bg-emerald-100 text-emerald-800' : 'bg-rose-100 text-rose-800' }}">
    {{ $item->enabled ? 'Habilitado' : 'Inhabilitado' }}
</span>
